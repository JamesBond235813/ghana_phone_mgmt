# 本轮任务总结：本地演示数据与岗位账号（历史版本）

> 本文件记录的是早期按工序拆分账号的演示数据，已被本轮岗位合并结果取代。
> 当前请以 [TASK_SUMMARY_2026-09-03_ROLE_MERGE.md](TASK_SUMMARY_2026-09-03_ROLE_MERGE.md)
> 和 `DEMO2-` 前缀数据为准；旧拆分账号保留为停用历史记录，不能登录。

日期：2026-09-03
项目：`/Users/jackbond/Desktop/ghana_phone_mgmt`

## 结果概览

已在本地 `ghana_phone` 数据库完成一套可重复执行的深圳—加纳手机流转演示数据，
并为每个主要作业岗位建立独立账号、角色权限和地点范围。原有超级管理员
`xiaojiang` 保持不变，没有清空或删除其它业务数据。

| 数据对象 | 本次 DEMO2 数量 |
| --- | ---: |
| 组织 | 2 |
| 地点 | 5 |
| 手机（主 IMEI） | 52 |
| 托盘 | 15 |
| 箱子 | 10 |
| 采购单 | 2 |
| 发运单 | 3 |
| 接收单 | 2 |
| 调拨单 | 4 |
| 销售单 | 4 |
| 退回单 | 2 |
| 维修单 | 4 |
| 盘点单 | 2 |
| 库存流水 | 277 |
| 岗位演示账号 | 16 |

所有演示数据使用 `DEMO2-` 前缀，脚本重复运行时按自然键更新，不会重复新增；
既有 `xiaojiang`、`super_admin` 和其它非 DEMO2 数据不受影响。

## 手机状态分布

| IMEI 序号 | 当前状态与用途 |
| --- | --- |
| 01–06 | 深圳库存，散台，供收购/装托演示 |
| 07–10 | 已装入 `DEMO2-TRAY-SZ-OPEN`，供装箱演示 |
| 11–16 | 已装箱（`DEMO2-BOX-SZ-READY`），供深圳发运演示 |
| 17–20 | 在途，仍保留深圳托盘/箱物理关系，发运单待加纳接收 |
| 21–24 | 加纳待验收，对应接收中发运单，逐台结果为空 |
| 25–34 | 加纳管理处库存（6 台在托盘、4 台散台） |
| 35–38 | 管理处→门店 A 在途，整箱调拨 |
| 39–40、52 | 门店 A 库存；其中 39–40 在托盘 |
| 41 | 门店 A 销售待确认，制单人与主管分离 |
| 42 | 已售出，尚未建立退回单，可直接演示退回建单 |
| 43 | 销售退回在途，整箱退回维修区 |
| 44 | 退回已接收、待送修，对应待接单维修单 |
| 45 | 维修中 |
| 46 | 维修完成待 QA 验收 |
| 47 | 维修验收通过，可再次销售 |
| 48 | 冻结待核查（外观损坏异常样本） |
| 49 | 报损/遗失样本 |
| 50 | 门店 B 库存，在托盘和箱内 |
| 51 | 门店 A→B 单台调拨在途 |

主 IMEI 按脚本中的 `demo_imei(1..52)` 生成且通过 Luhn 校验；部分手机另有
IMEI2，可用于验证双 IMEI 查询（序号 07、21、43、46）。

## 演示账号和权限

以下账号均为英文用户名，便于在 PC 或手机端登录；每个账号只绑定一个对应岗位
角色，权限范围按实际作业地点配置。所有这些演示账号统一使用本轮约定的本地
测试密码（仅开发演示，禁止用于生产）。

| 登录用户名 | 岗位角色 | 数据范围 | 主要权限 |
| --- | --- | --- | --- |
| `szbuyer` | 深圳收购验收 | 深圳演示仓；采购权限为深圳组织 | 查看手机、采购入库 |
| `sztray` | 深圳装托 | 深圳演示仓 | 查看/编辑手机、托盘管理 |
| `szpacker` | 深圳装箱 | 深圳演示仓 | 查看/编辑手机、托盘/箱管理 |
| `szdispatch` | 深圳发运 | 深圳演示仓 | 查看手机/发运单、发运、库存出库 |
| `ghreceive` | 加纳到货验收 | 加纳管理处 | 查看、拆箱、逐台验收、托盘/箱管理、库存接收、报表 |
| `ghwarehouse` | 加纳仓库拣货发运 | 加纳管理处 | 调拨出库、发运、托盘/箱管理、库存出库、报表 |
| `storeareceive` | 门店 A 收货 | 门店 A | 调拨收货、拆箱、逐台验收 |
| `storeaclerk` | 门店 A 销售员 | 门店 A | 销售建单、退回建单、提交盘点 |
| `storeasupervisor` | 门店 A 主管 | 门店 A | 销售审核/取消、店间调拨出库、盘点差异处理、报表 |
| `storeboperator` | 门店 B 串货员 | 门店 B | 店间调拨收发、销售、退回、盘点 |
| `returnpack` | 门店退回打包 | 门店 A | 退回建单、托盘/箱管理、库存出库 |
| `ghreturn` | 加纳退回接收分诊 | 管理处；维修建单范围另授维修区 | 退回接收、拆箱验收、托盘/箱管理、库存接收、维修建单 |
| `repairintake` | 维修建单 | 维修区 | 查看手机、建立维修单 |
| `repairtech` | 维修技师 | 维修区 | 接收维修、填写维修结果 |
| `repairqa` | 维修 QA | 维修区 | 维修结果验收、报表 |
| `auditor` | 报表审计盘点 | 全局（只读/盘点） | 查看手机/报表、审计日志、盘点及差异处理 |

角色权限（`RolePermission`）与用户数据范围（`UserScope`）均已写入；没有给一线
账号授予 `user:manage` 或 `role:manage`。现有超级管理员仍为
`xiaojiang`，其原有密码和全局权限未被演示脚本覆盖。

## 关键可操作单据

- `DEMO2-SHIP-SZ-GH-READY`：深圳已发运、加纳尚未建立接收单；用 `ghreceive` 演示建立接收单和拆箱。
- `DEMO2-SHIP-SZ-GH-CHECKING` / `DEMO2-RCV-GH-CHECKING`：4 台逐台验收待处理。
- `DEMO2-TRF-GH-STORE-A-READY`：整箱调拨在途；用 `storeareceive` 演示收货。
- `DEMO2-SALE-STORE-A-PENDING`：用 `storeasupervisor` 演示制单人与审核人分离。
- `DEMO2-RET-STORE-A-TRANSIT`：用 `ghreturn` 演示管理处接收退回。
- `DEMO2-REP-SUBMITTED`、`DEMO2-REP-ACTIVE`、`DEMO2-REP-QA`：分别演示维修接单、维修中和 QA 待验收。
- `DEMO2-ST-STORE-B-DIFF`：包含一个未建档的额外 IMEI，差异状态为 `OPEN`，可用审计/主管账号处理。

## 代码与服务变更

1. 重写 `backend/scripts/seed_demo_data.py`：
   - 组织、地点、手机、托盘、箱、业务单据、关系和 277 条有前后状态的库存流水；
   - 固定自然键、幂等 upsert、事务包裹，兼容重复执行；
   - 直接使用 scrypt 哈希写入本地演示密码（后台创建用户接口仍保留正式的 8 位密码规则）；
   - 不调用随机单号的业务接口，避免初始化过程中生成不可重复或不完整的流水。
2. 更新 `backend/app/api/documents.py`：发运、调拨、退回单据现在按来源或目的地
   任一可见范围返回，收货人员可以在自己的工作台看到入站任务。
3. `backend/README.md` 增加了从 `backend` 目录运行演示初始化的说明。

## 验证记录

- 迁移版本：`0008_usernames`。
- 数据一致性检查通过：IMEI/IMEI2 唯一且校验通过；手机—托盘、托盘—箱子的活动
  关系唯一且与 `current_*` 字段一致；地点与组织一致；各单据数量等于明细数量；
  发运单与对应接收单的手机集合完全一致；所有明细 IMEI 快照匹配手机主 IMEI；
  盘点状态只使用 `OPEN`/`COMPLETED`。
- 16 个演示账号逐个通过密码登录和 `/auth/me` 检查；每个角色的权限都有对应
  `UserScope`。`xiaojiang` 原密码登录仍通过。
- 目的地入站可见性验证通过：`ghreceive` 可见加纳待接收发运单，
  `storeareceive` 可见管理处→门店 A 在途调拨，`storeboperator` 可见门店 A→B 在途调拨。
- 后端测试：`16 passed`；后端健康检查：`GET http://127.0.0.1:8000/health` 返回 200。
- 重跑脚本后数量保持不变（手机 52、托盘 15、箱 10、库存流水 277 等）。

## 本地服务地址

- 手机端：<http://127.0.0.1:5173>；局域网：<http://192.168.1.192:5173>
- PC 端：<http://127.0.0.1:5174>；局域网：<http://192.168.1.192:5174>
- 后端健康检查：<http://127.0.0.1:8000/health>

当前本机 MySQL 实际报告版本为 9.3.0（项目目标写的是 MySQL 8.x）；本轮迁移、
读写及 API 验证均通过，但上线前仍建议在正式 MySQL 8.x 环境做兼容性回归。
