# 登录页响应式视觉审查总结（2026-09-04）

## 本轮范围

- 只读审查 `frontend-pc` 未登录登录页。
- 使用本地 Vite 前端的实际渲染结果，覆盖 1280×720、1920×1080、3420×1970、800×600；另补测 800×768、900×700、1024×768、1366×768 边界。
- 未修改 `App.vue`、`style.css` 或其他业务源码。

## 实测结论

| 视口 | 关键结果 |
| --- | --- |
| 1280×720 | 无横/纵滚动；卡片约 440×441 px，内边距约 30.7 px；标题 25 px，输入约 45 px，整体比例协调。 |
| 1920×1080 | 无滚动；登录内容组约 1132 px 宽并居中；卡片约 440×452 px、内边距 36 px；宽屏未被拉伸。 |
| 3420×1970 | 无滚动；内容组仍约 1132 px、居中，占屏幕约三分之一宽度；符合宽屏不铺满要求，但可按产品偏好将总组宽放宽至 1200–1320 px。 |
| 800×600 | 无滚动；流程/收益说明隐藏；标题 42 px，卡片约 460×366 px，底部保留约 22 px；适合小工位屏。 |
| 800×768 | 流程/收益说明恢复显示，页面总高约 814 px，溢出约 46 px；是当前唯一需要关注的一屏边界。 |

## 建议

- 1280 px 及以上无需新增密度层，现有 `clamp`、最大列宽和桌面密度层已消除巨幅卡片问题。
- 若要求 800×768 也严格一屏，可在 `@media (min-width:761px) and (max-width:900px)` 下隐藏 `.login-hero .login-flow`、`.login-hero .login-benefits`，或把隐藏条件从 `max-height:700px` 放宽至约 760px；这是可选优化，不能影响 800×600 的现有布局。
- 超宽屏若觉得留白过多，可将 `.login-page` 两列从 640+440 调整到约 680+480，保持 `justify-content:center`；当前没有溢出或可用性缺陷，建议先保持稳定。

## 验证方式

- 通过 Chrome DevTools Protocol 设置视口并读取 `.login-page`、`.login-hero`、`.login-card`、标题、表单控件的实际边界和计算样式。
- 四个目标尺寸均确认 `document.documentElement.scrollWidth/scrollHeight` 不超过视口（800×768 边界除外）。
