# 本轮任务总结：按实际岗位合并角色、权限、页面与数据模型

日期：2026-09-03
项目根目录：`/Users/jackbond/Desktop/ghana_phone_mgmt`

## 1. 本轮目标

根据最新岗位分工，将原来按单个工序拆开的账号和入口改为符合现场实际的岗位边界：

1. 深圳收购验收、装托盘、装箱由同一个深圳综合作业岗位连续完成；
2. 深圳发运由独立的深圳发运岗位完成；
3. 加纳到货验收、仓库拣货/调拨、退回分诊、维修建单和维修 QA 由同一个加纳管理处综合作业岗位承接；维修技师仍单独负责接单和填写维修结果。

## 2. 已落地的有效岗位和账号

| 岗位 | 账号 | 主要权限/范围 | 明确限制 |
|---|---|---|---|
| 深圳仓综合作业员 | `szops` | `purchase:create`、`phone:edit`、`tray:manage`、`box:manage`；深圳收货/包装地点 | 不可加纳收货、门店调拨或国际发运 |
| 深圳发运员 | `szdispatch` | `shipment:dispatch`、`shipment:view`、`inventory:issue`；深圳发运地点 | 不负责采购验收和包装 |
| 加纳管理处综合作业员 | `ghops` | 管理处：到货拆箱/验收、拣货调拨、退回接收；维修区：维修建单和 QA；两处均可按授权查看手机、容器和报表 | 不含 `shipment:dispatch`；不能代替维修技师接单或填写维修结果 |
| 门店 A 收货员 | `storeareceive` | 调拨收货、拆箱拆托、逐台清点；门店 A | 仅门店 A |
| 门店 A 销售员 | `storeaclerk` | 销售制单、销售退回发起、盘点提交；门店 A | 不能销售审核或处理差异 |
| 门店 A 主管 | `storeasupervisor` | 调拨发起、销售审核/取消、盘点差异处理；门店 A | 制单人与审核人分离规则仍生效 |
| 门店 B 串货员 | `storeboperator` | 店间调拨收发、销售、退回和盘点；门店 B | 仅门店 B |
| 门店退回打包员 | `returnpack` | 退回手机的托盘/箱装载与发运 | 不能接收退回或做维修结论 |
| 维修技师 | `repairtech` | `repair:receive`、`repair:update`；加纳维修区 | 不能做维修 QA、销售审核 |
| 审计盘点员 | `auditor` | 报表、审计、盘点和差异复核 | 默认只读 |

以上岗位账号为本地测试账号，密码已隐藏。超级管理员登录凭据不写入项目文档。

历史拆分账号（例如 `szbuyer`、`sztray`、`szpacker`、`ghreceive`、`ghwarehouse`、`ghreturn`、`repairintake`、`repairqa`、`ghanaops`）没有删除，但已停用、解除角色和数据范围，登录返回 401；这样保留审计历史，同时避免旧账号继续获得重复或过宽权限。

## 3. 数据库和迁移

- `roles` 表增加 `is_active`、`work_group`、`description`，用于归档历史岗位、稳定识别工作组和向前端提供岗位说明。
- 新增 Alembic `0009_merge_operational_roles`：先把旧角色用户重挂到合并后的 canonical role，再清空旧角色权限并标记为 inactive；不删除用户或历史流水。
- 新增 Alembic `0010_ghana_dispatch_boundary`：明确加纳综合岗位不拥有跨境 `shipment:dispatch`；加纳管理处向门店使用 `transfer:create` + `inventory:issue`。
- 现有 `permissions`、`role_permissions`、`user_roles`、`user_scopes` 继续作为 RBAC + 地点数据范围的权威来源，没有用前端角色名称代替后端授权。
- `ghops` 的接收/调拨/退回权限只落在管理处地点，`repair:create`/`repair:approve` 只落在维修区；手机、托盘、箱子和报表查看范围覆盖两处，符合一个岗位跨两个物理区域工作的实际情况。
- `GET /auth/me`、密码登录和 `/admin/roles` 返回角色代码、工作组、职责、操作元数据和权限；后台默认只列启用角色，禁止把停用角色重新分配给新用户。

## 4. 页面、交互和作业入口

PC 端和手机 PWA 均已按岗位合并工作台：

- 深圳岗位显示一张“深圳收购仓作业”卡片，步骤为“收购验收 → 装入托盘 → 装箱封存”；
- 加纳岗位显示一张“加纳综合仓作业”卡片，按账号实际权限显示“建立到货单 → 逐台到货验收 → 门店拣货调拨 → 退回分诊 → 建立维修单 → 维修 QA”；不具备技师权限时不会把建单成功误显示为可以立即维修或 QA；
- 深圳发运、门店销售/收货、维修技师接单/填维修结果保持独立入口；加纳综合岗位的“装托盘/装箱”仍保留为仓内重装的辅助入口，因为加纳到货和退回现场确实需要重新装载；
- 装托盘、装箱均为“先扫目标容器，再连续扫明细”：手机端先扫托盘/箱码再扫 IMEI/托盘码，PC 端支持扫码枪键盘输入和 Enter 连扫；摄像头不可用时可手工录入；
- 任务切换时按当前权限重新选择工作地点，管理处收货和维修区建单不会串用地点；目的地点按门店/仓库和组织过滤；
- 工作流步骤使用服务端权限和地点范围双重判断，前端隐藏只是体验优化，后端 API 仍会拒绝越权；生成的收货单号、退回单号会在连续步骤间保留；
- 维修建单完成后，如果账号不是维修技师，页面明确提示等待 `repairtech` 接单和提交维修结果，随后再由 `ghops` 进入 QA；退出登录会清理工作流、扫描清单、单据号和操作幂等键。

## 5. 业务和数据完整性修正

- 加纳退回采用“管理处接收 → 退回分诊拆托 → 转入维修区”的跨地点路径；维修单保存来源托盘快照，手机当前地点转到维修区，不把可复用的退回箱错误搬成维修容器。
- 跨地点分诊扫描箱时校验箱子启用状态、接收地点、非空，以及箱内每个托盘仍启用且位于接收地点，阻止陈旧容器关系导致重复分诊。
- 统一增加 IMEI2 别名解析：装托、查询、到货验收、调拨收货、销售退回创建/接收、维修和盘点调整都能扫描 IMEI1 或 IMEI2；同一台双卡手机在同一批次用两个 IMEI 扫描会被拒绝。
- 前端普通 POST 不再复用上一次操作的持久化幂等键；只有明确开始的现场操作或离线草稿重试才复用同一键，避免无关请求被错误缓存。
- 单据列表按来源或目的地任一授权范围显示，接收人员可以看到发给自己的入站任务；金额字段仍按报表范围隐藏。

## 6. 本地样本数据

已在本地 `ghana_phone` 数据库幂等重跑 `DEMO2-` 样本，不清理其他业务数据：

- 2 个组织、5 个地点（深圳仓、加纳管理处、两家门店、维修区）；
- 52 台手机、15 个托盘、10 个箱子；
- 3 个跨境发运单、2 个到货/接收单、4 个门店调拨单、2 个销售退回单、4 个维修单和 2 个盘点单；
- 当前活动手机—托盘关系 32 条、托盘—箱关系 11 条；`DEMO2-REP-SUBMITTED` 只有一个来源托盘快照，未残留旧版本的维修容器行；
- 样本包含在途、到货待验收、管理处库存、门店库存、销售待审核、退回在途、维修中、维修待 QA、冻结和报损等状态，方便逐个岗位登录演示。

种子命令：

```bash
cd /Users/jackbond/Desktop/ghana_phone_mgmt/backend
PYTHONPATH=. ../.venv/bin/python scripts/seed_demo_data.py
```

## 7. 验证结果

- `PYTHONPATH=. ../.venv/bin/pytest -q`：**23 passed，2 warnings**；
- `npm run build`（手机端）：成功，只有原有单 chunk 超过 500 kB 的 Vite 提示；
- `npm run build`（PC 端）：成功；
- `PYTHONPATH=. ../.venv/bin/alembic check`：`No new upgrade operations detected`；
- `python -m compileall`：通过；
- `git diff --check`：通过；
- 11 个有效账号均可登录；旧拆分账号返回 401；`ghops` 的 `/auth/me` 不含 `shipment:dispatch`；越权收货、维修建单和采购入库接口均返回 403；
- 后端已重启并通过 `GET /health`，当前监听 `127.0.0.1:8000`；两个 Vite 前端均返回 HTTP 200。

## 8. 当前访问地址

- 手机端（本机）：<http://127.0.0.1:5173/>
- 手机端（局域网）：<http://192.168.1.192:5173/>
- PC 端（本机）：<http://127.0.0.1:5174/>
- PC 端（局域网）：<http://192.168.1.192:5174/>
- 后端健康检查：<http://127.0.0.1:8000/health>

## 9. 边界和后续事项

- 本机 Homebrew MySQL 实际版本为 **9.3.0**，不是要求中的 8.x；本轮迁移和接口已在该实例验证，使用的 SQL/ORM 特性为 MySQL 8 兼容范围，正式部署到 MySQL 8 仍建议做一次真实升级演练。
- 短信登录目前是适配器占位；Refresh Token、照片附件、复杂离线冲突审批和完整报表仍属于上线前后续工作。
- `repair_orders.return_order_id` 当前是一条退回单对应一张维修单；若将来一个退回单要拆成多批维修，需新增分诊批次模型，不能用重复建单绕过唯一约束。
- 合并岗位迁移的 downgrade 保持 no-op，因为历史拆分后无法安全推断每个用户应恢复到哪个岗位；如需回滚必须人工审核。
- 当前改动未提交 Git commit，工作区中的代码和总结文件均保留供继续验收。
