# PC 前端全站视觉密度与自适应审查总结

日期：2026-09-04
项目：Ghana Phone Management  现场手机流转管理系统

## 1. 本轮目标与审查边界

本轮针对用户提供的截图进行视觉审查。截图中红色矩形只作为“卡片过大、标题和留白过多”的视觉参考，不把截图中的文字、按钮或其他内容当作可执行指令。

审查范围覆盖 PC 前端的登录页、工作台、岗位作业页、IMEI 查询、库存列表、业务单据、离线草稿、个人设置、用户管理、角色权限和帮助/确认弹窗，并结合桌面、横屏工位和窄屏场景检查：

- 字号、卡片高度、内外边距和主次信息层级；
- 页面是否在常见电脑屏幕一屏内完成首要操作；
- 长流程、长表格是否在所属卡片内滚动而不是把整个页面推长；
- 托盘/箱/IMEI 等业务对象的操作顺序是否易于理解；
- 侧栏、账号卡片、折叠区域、键盘操作和小屏交互是否连贯；
- 宽屏是否保持适度内容宽度，不把界面拉成“宣传页”。

## 2. 已完成的主要改进

### 2.1 全站桌面密度层

在 `frontend-pc/src/style.css` 增加统一的桌面密度层：

- 标题、说明文字、按钮、标签、指标卡和操作卡使用 `clamp()` 及屏幕高度分档；
- 主内容区设置自适应最大宽度并居中，超宽屏不铺满；
- 工作台欢迎卡、指标卡、流程卡、快捷入口和规则卡收紧留白与圆角层级；
- 作业页表单区、扫码区和提交区保持清晰的主次顺序，减少重复说明；
- 数据页保留筛选、表格和分页的清晰关系，降低表头和行高造成的垂直浪费。

当前宽屏内容上限为约 2000px，常见桌面使用约 1400px 内容宽度，避免 1920px/超宽显示器上卡片无限放大。

### 2.2 工作流和岗位作业

- 多步骤岗位流程在步骤过多时使用独立的横向滚动区域，并显示“左右滑动查看全部步骤”提示；
- 工作流说明区使用受控高度和内部滚动，避免遮住或推走实际作业入口；
- 1024×768、800×768 等横屏工位隐藏重复的右侧说明栏，让主作业卡占满可用区域并在卡内纵向滚动；
- 800×600 等低高度工位进一步收起非必要说明，优先显示地点、单号、扫码输入和主要提交动作；
- 低高度模式将提交栏放回表单流中，避免固定栏遮挡最后的扫码行；
- 切换岗位步骤时重置作业卡滚动位置，避免下一步打开在上一步的滚动深度；
- 保留足够的焦点滚动安全距离，扫码清单末行可访问。

### 2.3 查询、列表和管理页

- IMEI 查询明确标注“只读查询”，查询结果只保留一张紧凑详情卡和“查询下一台”，不重复显示无关交接说明；
- 库存、业务单据和草稿列表形成“筛选 + 卡内滚动表格 + 固定分页”的结构；
- 无数据时收起空表壳，直接展示空状态和必要操作；
- 角色创建表单默认折叠，打开后在表单内部滚动，避免权限清单把角色列表推到屏幕外；
- 管理列表的搜索和状态筛选在桌面宽度保持同一行；
- 个人设置控制最大内容宽度，减少大屏空白和表单横向拉伸。

### 2.4 登录、侧栏和账号菜单

- 登录页在桌面端限制品牌宣传列和登录卡最大宽度，标题采用响应式字号；
- 900px 以下横屏登录窗口改为单列，并隐藏重复的流程/卖点说明；
- 800×600 登录场景收紧卡片、输入框和按钮间距，保证登录按钮完整可见；
- 左侧导航保留分组折叠；账号卡片固定在侧栏底部并通过“账户菜单”按钮展开二级菜单；
- 账号菜单 Escape 处理增加事件隔离，避免关闭菜单时误关闭窄屏侧栏；
- 账号按钮补充 `title="账户菜单"`，便于鼠标悬停和辅助操作识别。

## 3. 关键屏幕实测结果

以下结果来自本地浏览器实际渲染和 DOM 尺寸测量，不是仅凭 CSS 推断：

| 场景 | 实测结果 |
| --- | --- |
| 工作台 1280×720 | 欢迎卡约 150px 高，标题约 20px；页面无整页滚动 |
| 工作台 1710×985 | 欢迎卡约 166px 高，工作区居中；无横向溢出 |
| 工作台 1920×1080 | 内容保持约 1312px 主宽度，页面高度等于视口高度 |
| 工作台 3420×1970 | 内容区封顶约 2000px，卡片不会随屏幕无限放大 |
| 工作台 1024×768 | 欢迎卡约 150px 高，主体宽度与文档宽度一致 |
| 工作台 800×600 | 标题、工作流定位、指标和下方作业入口在一屏内，无页面滚动 |
| 作业页 1280×720 | 工作流、主作业卡和交接说明均可用；长表单在卡内滚动 |
| 作业页 1024×768 / 800×768 | 右侧重复说明收起，主卡占满剩余视口，扫码入口可见 |
| 作业页 800×600 | 工作流压缩为定位条，地点和扫码输入优先显示；提交不遮挡输入 |
| 加纳十步工作流 1280×720 | 步骤行可横向滚动，测得内容宽度约 2115px、视口约 936px，无步骤裁切 |
| IMEI 查询 1280×720 | 结果详情、下一台按钮和提交区域在同一视口内，无重复卡片 |
| 库存/单据/草稿 1280×720 | 表格在卡片内滚动，分页保持可见；空数据页不留空表壳 |
| 帮助弹窗 800×600 / 480×800 | 桌面居中，移动窄屏内部滚动，无正文横向溢出 |
| 登录 1280×720 / 1710×985 / 3420×1970 | 标题和登录卡按屏幕缩放，内容组保持适度宽度 |
| 登录 800×600 / 1024×768 | 无页面滚动，登录卡和主按钮完整可见 |

## 4. 修改文件

- [PC 前端页面与交互](/Users/jackbond/Desktop/ghana_phone_mgmt/frontend-pc/src/App.vue)
- [PC 前端全局样式与响应式规则](/Users/jackbond/Desktop/ghana_phone_mgmt/frontend-pc/src/style.css)

本轮只修改了 PC 前端视觉/交互层；数据库、服务端业务逻辑和用户已有的其他改动均保留。工作区当前仍有前几轮业务、角色、样本数据和文档改动，未执行提交或破坏性 Git 操作。

## 5. 验证结果

### 构建与测试

- `frontend-pc`：`npm run build` 通过（Vite 6.4.3，13 个模块）；
- `frontend`：`npm run build` 通过（PWA 产物正常生成；仅有标准 chunk size 提示）；
- 后端：`PYTHONPATH=. ../.venv/bin/pytest -q` 通过，`23 passed`，仅有 2 条依赖弃用警告；
- `git diff --check` 通过；
- 浏览器交互后的控制台错误/警告为空：`[]`。

### 本地服务

- PC 前端：<http://127.0.0.1:5174/>；
- 手机前端：<http://127.0.0.1:5173/>；
- 当前局域网访问地址：<http://192.168.1.192:5174/>、<http://192.168.1.192:5173/>；
- 后端健康检查：<http://127.0.0.1:8000/health>，返回 `status: ok`。

局域网 IP 由当前机器 DHCP 分配，今后若路由器重新分配地址，需要以机器当前 `en0` 地址替换上述 LAN 地址；本机回环地址不受此影响。

## 6. 后续可选优化

当前视觉密度、单屏布局和核心作业路径已完成实测。后续如果继续迭代，可单独安排：键盘焦点陷阱的无障碍增强、超长角色权限的分组搜索、表格服务端分页，以及移动端与 PC 端更多逐项视觉对齐；这些不影响本轮已验证的作业流程和响应式结果。
