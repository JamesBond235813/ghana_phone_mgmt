# PC 操作页交互与视觉修复总结（2026-09-04）

## 本轮范围

本轮只处理 `frontend-pc` 的现场操作页：深圳/加纳岗位工作流、提交栏与扫码区关系、免扫描阶段文案，以及 IMEI 追踪只读查询语义。未修改库存、业务单据、系统管理或数据库。

## 已完成

- 将岗位步骤包入 `.workflow-steps-viewport`。超过 4 步时使用单行横向滚动、可见滚动条、右侧渐变提示和“左右滑动查看全部步骤”提示，避免被旧的 `overflow:hidden` 截断。
- 给操作卡增加底部安全滚动空间（`scroll-padding-block-end:90px`、表单底部 84px），使 sticky 提交栏不会挡住扫码清单末尾。
- 为 `.no-scan-card p` 明确设置 11px 字号、1.5 行高和紧凑边距，避免继承 body 16px 导致免扫描卡片异常膨胀。
- IMEI 追踪改为只读查询：隐藏岗位交接侧栏，徽章和按钮改为“只读查询/查询 IMEI”，新增 `submitQuery()` 支持手输、扫码枪回车和按钮查询；查询结果自动滚动到详情卡，不再显示提交交接语义，也不会因已查询号码弹出离开批次确认。
- 查询结果卡片采用紧凑样式，并保留“查询下一台/清除”操作；开始新查询时清除旧详情，避免误读。

## 验证结果

- 在 `frontend-pc` 执行 `npm run build`：Vite 构建通过（13 modules transformed）。
- 1280×720：加纳十步工作流面板约 159px 高，步骤内容横向可滚动（内容宽约 2117px、可视区约 936px），提示可见且无底部截断；深圳三步流程面板约 144px 高。
- 1280×720：免扫描段落由 16px 降为 11px，卡片高度由约 117px 降至约 75px。
- 1280×720：使用样本 IMEI 查询成功，结果卡和轨迹详情自动进入可视区域；切换回工作台不再弹出“确认离开当前批次”。

## 注意事项

- 底部安全空间会让操作卡滚动到底部时保留一段可见留白，这是为 sticky 提交栏和扫码清单可读性预留的安全区。
- 当前改动依赖现有 Vue 工作流状态与 API 合约，未改变后端接口或业务数据。
