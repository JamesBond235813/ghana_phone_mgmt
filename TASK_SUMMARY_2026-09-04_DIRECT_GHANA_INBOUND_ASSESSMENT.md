# 本轮任务总结：深圳未录入时加纳直接入库能力评估

## 需求

评估以下场景当前是否可由系统支持：一箱手机已经运抵加纳，但深圳办公室没有提前录入/创建发运记录，加纳管理处能否直接将手机入库。

## 本轮检查内容

1. 检查库存采购入库 API 和领域服务：`POST /api/v1/inventory/purchase-receipts`、`backend/app/domain/purchase_service.py`。
2. 检查加纳跨境收货 API 和领域服务：`POST /api/v1/shipments/receiving/start`、`POST /api/v1/shipments/receiving/inspect`、`backend/app/domain/shipment_service.py`。
3. 检查加纳综合岗位的权限与地点范围：`backend/scripts/seed_demo_data.py`、`backend/app/core/roles.py`。
4. 检查 `inventory:receive` 是否有独立可调用的库存入库路由。

## 结论

当前版本**不能完整满足**“深圳未录入、加纳直接入库”这一场景。

- 加纳收货流程要求先存在深圳发起的 Shipment，且状态必须为“运输中”；随后才能按目的组织/地点创建 ReceivingOrder，再逐台验收。
- 收货服务会从 ShipmentItem 读取已存在的 `PhoneDevice`，因此未知 IMEI 不能通过现有跨境收货接口直接创建手机档案。
- `inventory:receive` 权限虽然已定义并授予加纳综合岗位，但当前代码中没有发现对应的独立 API/领域服务；它不能单独形成“无来源单据的现场入库”能力。
- 现有 `purchase-receipts` 确实可以创建未知 IMEI，但其服务固定把新手机状态写为“深圳库存”（`PhoneStatus.SHENZHEN_STOCK`），并且权限默认只给深圳综合作业员的深圳仓地点范围。因此即使管理员强行调用，也不符合加纳入库业务语义，不能视为已支持。

## 当前可行的替代做法

在不改代码的情况下，只能先由有权限人员补建深圳端采购/发运数据，再按正常 Shipment → 加纳接收 → 逐台验收流程处理；这会留下补录痕迹，但不能反映“加纳现场直接建档”的真实业务动作。

## 建议的产品改造方向

增加受控的“加纳异常到货/现场建档入库”流程，而不是放开普通采购入库：

1. 加纳综合岗位在管理处创建异常到货单，逐台录入/扫描 IMEI、品牌型号等资料。
2. 新手机初始状态应为“加纳待验收”，地点为加纳管理处，来源标记为 `GHANA_DIRECT_INBOUND` 或类似异常来源。
3. 由另一名主管/审计角色复核后转为“加纳管理处库存”；异常、重复 IMEI、已有系统档案必须阻断或进入差异处理。
4. 记录原始箱号、物流号、到货时间、照片/备注、操作人和复核人，并在 IMEI 时间线中形成不可变流水。
5. 前端应将该流程与正常跨境收货明确区分，避免操作员误把缺少深圳单据的货物当成正常收货。

## 效果

本轮未修改业务代码，仅新增本评估总结文件；结论基于当前代码静态检查和权限/状态模型核对。
