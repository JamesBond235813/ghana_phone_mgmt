# 本轮任务总结：PC 端导航、账户入口与自适应视口改造

## 1. 任务范围

本轮围绕 PC 前端所有主要页面进行了一次界面与交互审查，重点处理左侧导航、登录用户入口、不同屏幕尺寸下的页面占用，以及长列表/详情内容的承载方式。目标是让现场人员在笔记本、台式机和窄屏设备上都能快速找到当前岗位需要的功能，并尽量在一个固定工作区内完成作业。

## 2. 左侧导航与账户入口

- 导航重新按作业习惯分为三个可折叠分组：
  - **现场作业**：工作台、深圳收购仓作业、加纳综合仓作业、发运出库、销售出库、盘点核查。
  - **查询与协同**：库存列表、业务单据、离线草稿、IMEI 追踪。
  - **系统管理 / 审计记录**：管理员显示系统管理；只有审计查看权限时显示审计记录，避免给只读人员造成可配置系统的误解。
- 每个分组使用按钮、展开状态和明确的 `aria-expanded`/`aria-controls`，可鼠标点击，也可键盘操作。
- 桌面宽屏增加“收起侧栏”按钮。收起后保留图标、当前态和悬浮提示，侧栏缩为图标栏，不挤占主要作业区域；再次点击可恢复完整文字导航。
- 登录用户信息从原先的顶部大卡片移到左侧底部，改为“我的账号”按钮。点击后才展示姓名、账号、岗位、数据范围，以及个人资料、系统管理/审计、退出登录等二级菜单。
- 账户菜单支持 Escape 关闭、Tab 焦点循环、点击外部关闭和关闭后焦点回到触发按钮；切换页面或打开移动端抽屉时会自动收起，避免菜单残留。
- 删除页面标题区域重复显示的岗位和数据范围胶囊，减少已知信息的重复占位。

## 3. 视口与列表承载方式

- 应用外壳、侧栏和主工作区统一使用 `100vh/100dvh`，页面外层 `overflow:hidden`。浏览器主页面不再因为卡片堆叠而产生整页上下滚动。
- 工作台、作业页和查询页改为“固定工作区 + 卡片内部滚动”。需要查看大量内容时，只滚动对应卡片，不会把页面标题、地点选择器或扫码输入框一起滚出视野。
- 库存、单据、草稿和管理列表采用卡片内滚动；表头设置为 sticky，长表格不会失去字段含义。
- 详情抽屉改为固定在右侧的独立面板（移动端从右侧覆盖），打开详情时不会把整个列表页面向下推移；关闭按钮可直接获得焦点。
- 操作页的流程说明、作业步骤和扫码清单分别限制高度；笔记本高度较小时隐藏重复的说明性段落，保留地点、扫描、清单和提交等关键控件。步骤过多时在步骤卡片内横向滚动。
- 工作台的完整流转路径改成可展开详情；常见笔记本高度下默认隐藏重复流程图，让“待办/快捷作业”优先进入视线。
- 管理页的新建用户表单改为默认折叠，点击“新建用户”后在表单区域内部滚动；用户列表和权限编辑区域保持独立滚动。
- 内容容器最大宽度限制为 1440px，并在更宽显示器上居中，避免信息被拉得过散、操作距离过长。
- 对 1280×720、1366×768、1440×900 等常见 PC 尺寸进行了紧凑规则处理；对 1024、768、480、320 宽度进行了无横向溢出检查。移动端抽屉在窄屏内自动收窄。

## 4. 文案和控件精简

- “查询手机”统一改成更符合作业人员认知的“IMEI 追踪”，提示语明确为“输入 IMEI 查看全程轨迹”。
- “库存”页面标题统一为“库存列表”，列表说明改为当前加载范围，避免把受限查询结果误认为全库总数。
- 库存和单据筛选器补充了可见字段标签及控件语义：关键词、状态、地点、单据类型等；不再只依赖 placeholder 猜测输入内容。
- 扫码状态在有作业任务时显示“扫码输入已就绪”，没有任务时只显示“系统在线”，减少无意义的长提示。

## 5. 修改文件

- `frontend-pc/src/App.vue`：导航分组、收起侧栏、底部账户菜单、键盘/焦点行为、页面文案、流程和管理表单结构。
- `frontend-pc/src/style.css`：固定视口、响应式断点、图标栏、账户弹层、卡片内滚动、sticky 表头、固定详情抽屉、紧凑屏幕规则和移动端适配。
- `frontend-pc/README.md`：补充新的导航结构、账户入口、视口策略和列表交互说明。

## 6. 验证结果

- PC 前端 `npm run build`：通过（Vite 6.4.3）。
- 手机前端 `npm run build`：通过；仅保留已有的 JS 大 chunk 提示，不影响构建结果。
- 后端测试：`PYTHONPATH=. ../.venv/bin/pytest -q`，**23 passed, 2 warnings**。警告来自 Starlette/httpx 的兼容性提示，不是本轮改造引入的失败。
- `git diff --check`：通过。
- 浏览器回归：已登录超级管理员逐一打开 11 个主要页面，未出现错误提示、控制台错误或横向溢出；操作页、库存页、单据页、草稿页、IMEI 追踪页和管理页均保持外层无滚动，长内容在所属卡片内滚动。
- 视口检查：1280×720、1366×768、1440×900 下文档宽高与视口一致；移动宽度 768、480、320 下无横向溢出；1920 宽屏内容按 1440px 上限居中。
- 最终已清除浏览器测试用的临时视口覆盖，页面恢复到默认 1280×720 工作区并回到工作台，未提交任何测试表单或生成额外业务数据。

## 7. 当前运行状态与后续建议

- PC 前端：`http://127.0.0.1:5174/`（HTTP 200）。
- 手机前端：`http://127.0.0.1:5173/`。
- 同一局域网设备可访问：PC `http://192.168.1.192:5174/`、手机 `http://192.168.1.192:5173/`（本轮实测均返回 HTTP 200；局域网地址可能随网络变化）。
- 后端健康检查：`http://127.0.0.1:8000/health` 返回 `{"status":"ok","service":"Ghana Phone Management"}`。
- 本轮没有修改数据库表结构，也没有执行破坏性数据库操作。
- 后续可在真实扫码枪和实际低分辨率工位屏幕上做一次现场走查，重点观察扫码枪焦点保持、弹层与打印/浏览器缩放行为；如需要，可再针对具体岗位进一步隐藏无关导航项。
