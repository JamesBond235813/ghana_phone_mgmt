# PC 前端全页面审查与改进总结（2026-09-04）

## 1. 本轮目标

围绕深圳收购仓、深圳发运、加纳管理处、门店、退回分诊和维修 QA 的真实作业路径，对 PC 端登录页、工作台、所有现场作业、库存、单据、离线草稿、个人设置和系统管理页面进行逐页审查，并从新手员工的操作顺序、扫码枪使用、容器实物关系、跨班次交接、权限边界、响应式布局和可访问性角度修正交互。

## 2. 审查范围与方法

- 静态审查 `frontend-pc/src/App.vue`、`frontend-pc/src/style.css`、PC API 复用层和权限/地点筛选逻辑。
- 浏览器实际登录超级管理员，检查 12 个主导航页面：工作台、深圳收购仓作业、加纳综合仓作业、发运出库、销售出库、盘点核查、库存查询、业务单据、离线草稿、查询手机、个人设置、系统管理。
- 检查系统管理 5 个标签页：用户、组织、地点、角色、审计。
- 在 1280、1024、768、480、375、320 宽度下检查布局；验证移动菜单打开/关闭、焦点返回、横向溢出和键盘操作。
- 实测扫码输入、IMEI 校验、重复扫描、容器先扫、地点/目标地点前置条件、维修无来源建单、调拨部分收货、帮助弹窗、确认弹窗和管理页标签键盘导航。

## 3. 已完成的主要改进

### 3.1 工作台和页面信息架构

- 侧栏按“现场作业 / 查询与协同 / 账户与管理”分组，按权限隐藏无权页面；工作台显示岗位、授权地点、可见库存/单据和离线草稿数量。
- 首页增加“IMEI → 托盘 → 箱 → 交接”的流程图、作业入口、现场三条提醒和明确的下一步动作。
- 所有页面统一标题、面包屑、岗位/数据范围上下文、在线状态、加载状态、成功/错误提示和帮助入口。
- 库存、单据、草稿、后台列表增加搜索、状态/类型/地点筛选、每页数量、分页、空状态和详情抽屉；金额等字段继续由后端权限控制。
- 单据和手机详情使用人类可读的地点、状态、类型、时间线文字，避免只显示 ID 或英文枚举。

### 3.2 现场作业和容器交互

- 深圳综合岗位在同一张工作卡中按“收购验收 → 装入托盘 → 装箱封存”连续作业；装托/装箱强制先锁定目标托盘/箱子，再扫描内容物。
- 发运、调拨、销售、退回、维修建单支持“手机 / 托盘 / 箱子”三种装载单位，并在字段旁解释何时使用哪一种。
- 收货、到货验收、维修结果和 QA 阶段回到逐台 IMEI，避免把容器码误当手机验收记录。
- 地点、目标地点、单号和容器按实际顺序分段显示；选择地点后自动聚焦下一项，减少鼠标往返。没有授权地点时明确提示管理员，不再提供错误的兜底地点。
- 输入框补齐中文标签、必填/可选标记、示例占位符、操作说明、`autocomplete` 和输入模式；扫码框在上下文不完整时禁用并显示具体原因。
- 扫码枪回车只加入清单，不会意外提交表单；支持 15 位 IMEI 校验、IMEI2/别名、重复扫描提示、容器编码校验、清单逐项移除和清空确认。
- 容器单选框使用显式受控状态、原生 `change`、方向键/空格/回车和焦点迁移；已有清单时切换装载单位会先确认并清空，防止混码。

### 3.3 真实业务状态和交接

- 工作流步骤只有在接口真正成功且该阶段确实完成后才标记完成；部分扫描不会误解锁下一阶段。
- 明确“扫描记录、验收结论、封存、发出、交接、可售库存”之间的区别，并在操作卡右侧显示“现场操作口诀”和“交给下一岗位”。
- 到货验收的正常/异常结论按批提交，异常机不会被误入正常库存；正常机重新装托/装箱时校验容器关系。
- 调拨收货新增“完成本次收货 / 暂存已收，稍后继续”选择：未到齐时只登记实收，确认清点结束后才把短少记账并冻结，支持后续继续扫描。
- 维修建单显示维修技师交接提示；维修接收、维修结果、QA 复核分别使用对应单号和结论，避免综合岗位把技师工作伪装成已完成。
- 跨班次可点击虚线步骤，按提示输入已有单号或重新扫描实物；不会把上一步未提交的输入悄悄带到新阶段。
- “开始新批次”、切换页面、切换工作流、退出登录、重新扫描容器和清空清单均有明确确认边界；成功后提供复制单号、下一步/新批次按钮。
- 修正关键边界：加纳“建立维修单”的“关联退回单号”只是追溯字段、允许为空；现在不填来源单也能选择地点并直接扫描 IMEI 建单。

### 3.4 离线和幂等保护

- 离线草稿按登录用户隔离，保留地点、目标地点、容器、物流、销售价格、维修结论、调拨完成方式和工作流信息。
- 每个草稿/明细使用稳定幂等键；网络失败保存为待同步草稿，部分明细成功后只保留未处理项。
- 批量重试只处理网络失败的待同步项；业务冲突要求人工核对后重试，避免一键重复提交错误单据。
- 顶部在线/离线状态、草稿数量、同步状态和冲突原因均可见，断网时不会把本地录入误显示为业务已完成。

### 3.5 管理、权限和可访问性

- 用户、组织、地点、角色和审计页面统一表单/表格布局；高级数据范围用可视化构建器表达，仍支持 JSON 导入。
- 用户启用/停用、组织启用/停用、地点启用/停用均增加二次确认；角色页显示职责、工作组、权限数量和停用旧岗位提示。
- 管理标签页补齐 `role=tab`、`tabpanel`、`aria-controls`、`aria-labelledby`，支持左右/Home/End 键切换。
- 登录、密码显示/隐藏、所有作业字段、搜索框、分页、详情抽屉、帮助/确认弹窗和移动侧栏均有可读标签、键盘焦点和 Escape 关闭行为。
- 小屏幕侧栏改为抽屉式导航，关闭后焦点返回菜单按钮；表单改为单列、按钮全宽，桌面表格保持可横向滚动且不撑破页面。
- 增加减少动画支持、统一焦点轮廓、禁用态、状态颜色和空数据提示，降低新手理解成本。

## 4. 角色与页面入口（演示数据）

以下账号为本地测试账号，密码已隐藏：

| 账号 | 岗位 | 主要 PC 入口 |
|---|---|---|
| `szops` | 深圳仓综合作业员（收购验收 + 装托 + 装箱） | 深圳收购仓作业 |
| `szdispatch` | 深圳发运员 | 发运出库、单据/查询 |
| `ghops` | 加纳管理处综合作业员（到货验收、仓库拣货、退回分诊、维修建单/QA） | 加纳综合仓作业、单据/查询 |
| `storeareceive` | 门店 A 收货员 | 调拨收货、到货验收 |
| `storeaclerk` | 门店 A 销售员 | 销售出库、销售退回、盘点 |
| `storeasupervisor` | 门店 A 主管 | 调拨发起、销售复核、盘点差异 |
| `storeboperator` | 门店 B 串货员 | 店间调拨/收货、销售、退回、盘点 |
| `returnpack` | 门店退回打包员 | 退回发起、装托、装箱 |
| `repairtech` | 维修技师 | 维修接收、填写维修结果 |
| `auditor` | 审计盘点员 | 库存/单据/审计/盘点 |

超级管理员账号为 `xiaojiang`，密码不写入项目文档。页面入口和地点下拉框均按账号权限与数据范围动态过滤，历史拆分岗位保持停用，不会重新显示为可用入口。

## 5. 验证结果

- PC 构建：`frontend-pc npm run build` 通过（Vite 6.4.3，JS 约 220.79 kB，gzip 约 69.44 kB）。
- 手机端构建：`frontend npm run build` 通过（PWA 资源生成成功）；存在既有的单 chunk 大于 500 kB 优化提示，不影响当前运行。
- 后端测试：`23 passed, 2 warnings`。
- 后端 `compileall` 通过。
- `git diff --check` 通过。
- 浏览器主页面回归：12/12 页面标题正确、无错误横幅、无控制台 error/warn、桌面无横向溢出。
- 管理页回归：5/5 标签页可打开，ARIA 引用完整，无重复 ID。
- 响应式回归：1024、768、480、375、320 宽度下 body/document 宽度与视口一致；移动菜单打开/关闭和焦点返回正常。
- 扫码回归：地点未选时入口锁定，地点选择后自动聚焦；维修建单无退回单时可扫描 IMEI；容器先扫、重复扫描和 Enter 行为符合预期。

## 6. 当前服务地址

- PC 前端：<http://127.0.0.1:5174/>
- 手机/PWA 前端：<http://127.0.0.1:5173/>
- FastAPI：<http://127.0.0.1:8000/>
- 健康检查：<http://127.0.0.1:8000/health>（HTTP 200）

当前监听进程已确认：后端 `127.0.0.1:8000`，PC `*:5174`，手机端 `*:5173`。浏览器已恢复到超级管理员工作台首页。

## 7. 后续建议

- 手机端当前生产包仍有大 chunk 警告，可在后续迭代做路由/模块拆分。
- 复杂离线冲突审批、照片附件、打印托盘/箱标签、交接签名/封签核验、完整事件修订仍应按上线前设计文档继续实现。
- 本轮未提交 Git commit；工作区中已有的后端、数据库、移动端和文档改动均保留，未执行破坏性数据操作。
