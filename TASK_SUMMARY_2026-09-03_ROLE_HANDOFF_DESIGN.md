# 本轮任务总结：岗位作业、交接、权限与现场交互设计

日期：2026-09-03

## 本轮目标

在已有深圳—加纳—门店手机流转描述、代码审计和开源调研基础上，从每一个岗位的实际物理动作出发，明确承接上一工序、开展作业、形成记录、移交下一工序的完整设计，并将其转化为后续数据库、API、前端和 UAT 可执行的共同基线。

## 已完成事项

1. 扩充 `docs/ROLE_SOP_HANDOFF_DESIGN_2026-09-03.md`，形成从设计原则到完成定义的完整蓝图，内容包括：
   - 深圳采购/验收、装盘、装箱、发运；
   - 加纳到货、开箱开托、逐台验收、重装和上架；
   - 管理处向门店整箱/整托/单台发运与门店收货；
   - 店间串货；
   - 门店维修/破损退回、加纳接收、分诊、维修和独立 QA；
   - 主管、审计、承运人等交接责任。
2. 明确关键记录时点：每次扫描先写不可变扫描事件；装盘/装箱扫描立即形成临时成员关系；拣货扫描建立预留而不提前改变在途归属；到货、开封、逐台验收、封存、发出和签收按状态机分开确认。
3. 增加岗位—页面—按钮—记录矩阵，定义每个岗位看到的待办、主按钮、禁止动作、移交对象和证据。
4. 增加动作权限、数据范围、字段遮罩和制单/审核分离设计，覆盖 `work_session`、拣货、封签、收货、差异、维修 QA、报废和库存纠正。
5. 增加目标数据模型：`handling_units`、`unit_membership_events`、`operation_documents`、`manifest_lines`、`work_sessions`、`scan_events`、验收、交接、封签、差异、库存事件、预留、区域和附件等；同时列出数据库不变量、幂等和并发锁要求。
6. 增加手机端/PC 端信息架构、首次扫描规则、连续扫码反馈、异常抽屉、物理区域颜色、断网恢复、换设备继续和交接回执设计。
7. 增加异常场景矩阵、P0–P4 实施顺序、每阶段出口和真实业务验收场景；列出需在原型评审时确认的容量、封签、部分接收、双人审批等参数及建议默认值。
8. 将 Odoo/OCA、ERPNext、InvenTree、OpenBoxes、OpenWMS 的可借鉴边界和许可证注意事项写入主设计，并更新 `README.md` 的设计文档索引。
9. 根据复审补充无箱托盘直发/散台直发、盲收、承运人临时身份、跨班交接、离线候选事件、部分拆箱、容器复用周期、事件反向/修订语义、嵌套预留互斥和 U1–U15 验收清单。
10. 对旧的总体设计和交互附录增加“当前岗位设计为执行基线”的说明，消除“整批提交才首次记关系”等旧表述的歧义。

## 当前结果和效果

- 一线员工的目标操作被统一为“看待办 → 扫描当前目标容器 → 连续扫描内容物 → 按颜色放置 → 核对数量/异常 → 封存/交接”，不需要理解内部数据库状态名。
- “扫到一台手机就自然装入托盘并形成记录”被落实为即时事件和临时关系；最终封存只负责核数、快照和状态晋级，取消会产生反向事件而不是删除历史。
- 箱、托盘、手机可以整件或部分重组，但所有库存和差异均回到 IMEI 明细，容器复用不会覆盖历史。
- 发出、到货未开封、开箱开托、逐台验收、可用入库、维修分诊和门店销售被明确拆开，未核对、异常、维修和在途手机不会误进入可售库存。
- 前端手机端和 PC 端共享作业会话/API；重复扫描、并发占用、缺失、多出、错货、封签破损、断网和部分收货都有明确补救路径。
- 文档检查通过；从 `backend` 目录执行 `../.venv/bin/python -m pytest -q`，现有测试结果为 **16 passed**（仅有既有依赖弃用警告）。前端 `npm run build` 亦通过。
- 复核运行环境：后端 `127.0.0.1:8000/health` 和前端 `127.0.0.1:5173/` 均返回 200；数据库 `ghana_phone` 的 Alembic 版本为 `0008_usernames`、现有用户 1 个。当前本机 MySQL 服务报告版本 `9.3.0`，与项目约定的 MySQL 8.x 不一致，生产部署前应在 MySQL 8.x 做兼容性回归。

## 本轮未做的事项

- 没有修改后端业务代码、前端代码或数据库结构；本轮交付是先行的业务/交互/权限/数据设计，避免在状态和责任尚未确认时直接改表。
- 现有本地服务和数据库保持此前已验证的运行状态；新蓝图尚未执行 Alembic 迁移。

## 下一步建议

1. 先与现场负责人确认文档第 17 节的业务参数，尤其是托盘容量、质量混托、封签和双人审批规则。
2. 按 P0 实现作业会话、扫描事件、容器关系事件、预留/并发锁、统一权限范围和审计脱敏。
3. 按 P1 打通深圳→加纳的真实闭环，再用少货、多货、封签破损、部分拆分和重复 IMEI 做 UAT；通过后再进入门店和维修流程。
4. 原型评审时同时让一名手机端作业员和一名 PC 主管按真实箱/托/手机演练，不以单纯接口成功作为验收。

## 文件

- 主设计：[docs/ROLE_SOP_HANDOFF_DESIGN_2026-09-03.md](docs/ROLE_SOP_HANDOFF_DESIGN_2026-09-03.md)
- 总体技术/领域基线：[docs/SYSTEM_DESIGN.md](docs/SYSTEM_DESIGN.md)
- 开源调研：[docs/OPEN_SOURCE_INVENTORY_RESEARCH_2026-09-03.md](docs/OPEN_SOURCE_INVENTORY_RESEARCH_2026-09-03.md)
