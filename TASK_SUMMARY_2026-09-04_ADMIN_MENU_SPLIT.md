# 本轮任务总结：系统管理与用户管理菜单拆分

## 需求

将左侧菜单原“系统管理”拆分为“系统管理”和“用户管理”：系统管理负责系统业务配置；创建好的用户在用户管理页面查看和维护。

## 已完成

- PC 端左侧管理菜单拆分为：
  - **系统管理**：系统业务配置入口，承载组织、地点、角色、权限和审计等配置页面。
  - **用户管理**：用户创建、用户列表、岗位分配和数据范围维护。
- 用户管理页面进入时自动定位到“用户”页签。
- 系统管理页面进入时自动定位到角色/组织/地点/审计等系统配置页签，不再把用户列表作为默认内容。
- 根据权限分别显示菜单：只有 `user:manage` 的账号可见用户管理；具备 `role:manage` 或 `audit:view` 的账号可见系统管理。
- 保留原有后台 API、权限代码和数据模型，不改变用户创建、角色分配和组织地点配置逻辑。
- 账户菜单中的管理入口仍可用，现有“系统管理”快捷入口保持兼容。

## 验证

- `frontend-pc` 生产构建通过：Vite build successful。
- 岗位权限回归测试：`tests/test_merged_role_scope.py` 3 passed。

## 结果

管理员登录后，左侧菜单会看到两个清晰入口。通过“用户管理”创建的用户会显示在用户管理页面的用户列表中；系统业务基础配置则在系统管理页面维护。
