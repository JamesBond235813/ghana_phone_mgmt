# Ghana 退回接收—分诊—维修建单审阅总结

日期：2026-09-03

## 本轮目标

审阅“门店退回 → 加纳管理处接收 → 管理处分诊 → 维修区建单”的实际作业路径，确认退回目的地点（`return destination`）与维修建单地点（`repair location`）不同时，系统仍能由同一加纳综合岗位顺手、可追溯地完成交接，并检查重复提交、幂等键和托盘/箱约束。

## 已完成的修正

- `backend/app/domain/repair_service.py`
  - 允许已完成退回单在退回目的地（通常 `GH-MGMT`）接收后，于同组织启用的维修/工作区（`GH-REPAIR` 等）创建维修单。
  - 跨地点分诊时逐台将手机从管理处当前位置移交至维修区，保持 `WAITING_REPAIR` 状态；若手机仍在退回托盘中，先结束活动托盘关系。
  - 记录“退回分诊拆托”和“退回分诊送维修”两条库存流水，再记录“创建送修单”，避免把退回单接收记录错误地直接写成维修区到货。
  - 托盘/箱作为来源快照留在 `repair_containers`；容器本身仍归管理处，避免维修建单误把可复用的退回箱/托盘虚拟搬走。
  - 跨地点容器扫描必须满足：容器存在、启用、位于退回接收地点且非空；展开出的手机仍逐台校验组织、地点、状态和退回单归属。
  - 拒绝同一物理手机通过 IMEI1/IMEI2 重复提交，并拒绝同一退回单再次建立维修单（`RepairOrder.return_order_id` 当前为一对一约束）。

- `backend/app/api/repair.py`
  - 跨地点建单额外要求当前账号拥有退回接收地点的 `return:receive` 范围，防止只有维修区建单权限的账号凭退回单号搬移任意库存。
  - 幂等键被重复用于不同请求/操作时返回明确的 HTTP 409，而不是未处理异常导致 500；同一键的网络重试继续返回原始结果。

- `frontend/src/App.vue`、`frontend-pc/src/App.vue`
  - 加纳综合流程在退回接收后自动带出退回单号到“建立维修单”步骤，并默认逐台扫描 IMEI。
  - 页面提示维修建单是“管理处 → 维修区”的分诊交接，原退回箱/托盘不直接作为维修区容器搬移；离线草稿保留工作流标识，重试时继续传递 `return_no`。

## 验证结果

在 `backend` 目录执行：

```bash
PYTHONPATH=. ../.venv/bin/pytest -q
```

结果：`21 passed, 2 warnings`。新增覆盖包括：

- 管理处接收后跨地点维修建单、拆托、手机位置迁移和三段流水；
- 同一退回单重复建单保护；
- 错地点或空退回容器拒绝；
- `/repairs` 同一 `X-Idempotency-Key` 重试返回同一结果。

前端构建与格式校验此前已通过：

- `frontend`: `npm run build` 成功；
- `frontend-pc`: `npm run build` 成功；
- `git diff --check` 成功。

## 当前边界与后续建议

当前 `repair_orders.return_order_id` 是唯一字段，因此一张退回单只能关联一张维修单；如果后续需要把同一退回单拆成“维修、可售恢复、冻结/报损”等多个独立批次，应改为允许一对多并增加分诊批次/明细状态。现阶段一个维修单可包含该退回单的全部或部分手机，但同一退回单不能再次新建第二张维修单。

本轮未新增数据库列或迁移；修正复用了现有地点、容器关系、维修单和库存流水结构。部署到正在运行的服务前需要重启后端进程，以加载新的 domain/API 代码。
