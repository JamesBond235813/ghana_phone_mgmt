# PC 前端源码审查与低风险修正（2026-09-03）

## 本轮范围

本轮针对 `frontend-pc/src/App.vue`、`frontend-pc/src/style.css`、`frontend-pc/src/api.ts`（该文件转出共享 API）以及 PC 前端的路由/状态切换逻辑进行了静态审查。重点从仓库操作员的实际动作出发，检查登录、工作台、深圳综合岗位、加纳综合岗位、单项作业、库存/单据查询、离线草稿、个人设置、系统管理九类页面/状态的控件、文案、权限可见性、输入校验、扫码连续作业、错误恢复和窄屏表现。

## 已直接修正

### 1. 防止同一页面连续批次命中旧幂等结果

`App.vue` 的成功提交路径现在会：

1. 展示后端返回的业务单号（采购单、发运单、接收单、调拨单、销售单、退回单、维修单或盘点单）；
2. 清空本批次编码；
3. 释放本批次 `X-Idempotency-Key` 并创建下一批次的新 key。

失败或离线时仍保留原 key，离线草稿重试不会因为重复提交而产生二次业务记录。这个修正尤其影响同一岗位在一个工作台连续处理多托盘/多箱的场景。

### 2. PC 工作台可读性、键盘和窄屏增强

在 `style.css` 增加了低风险的通用增强：

- 所有输入、选择框和主要按钮统一可操作高度；
- `:focus-visible` 明确键盘/扫码枪焦点，不依赖鼠标；
- 长单号、错误信息和标题自动换行；
- 面板内表格允许横向滚动，避免列挤压后看不清 IMEI；
- 表格行 hover、空状态、状态徽章、字段标签和帮助文字的样式预留；
- 工作流步骤有更清楚的最小高度和横向滚动条；
- 900px、720px、460px 三档响应式规则：侧栏在窄屏变为横向导航，表单/卡片改为单列，扫码按钮改为整行；
- 登录页在小屏幕不再因固定宽度和内边距产生横向溢出；
- 尊重 `prefers-reduced-motion`，减少动效对现场操作的干扰。

## 审查结论（按优先级）

### P0/P1：建议主模板重构时一并落地

1. **字段缺少可见标签。** 当前大量表单只依赖 placeholder（当前地点、组织、目标地点、业务单号、价格等），输入内容后提示消失；小白员工和屏幕阅读器难以确认字段含义。建议每个控件采用“字段名 + 示例/规则 + 控件”的三层结构，必填项显示 `必填`，只读组织字段使用文本徽章而不是可编辑样式。
2. **工作流步骤没有完成/待处理/异常状态。** 目前步骤主要靠当前高亮，无法快速判断上一环节是否已经提交、是否存在待交接单据。建议为每步显示状态图标、单号、已扫数量和最后提交时间；不满足前置单据时禁用后续步骤并说明原因。
3. **离开未提交批次会静默丢失。** 切换菜单、切换阶段或退出登录会清空当前扫描列表。建议在 `scanned.length > 0` 时弹出“保留草稿/清空并离开/取消”，并提供快捷键提示。
4. **库存、单据、草稿表缺少表头、筛选和分页/计数。** 当前表格仅展示数据行，员工无法确认列含义；500 行上限也没有“已显示/总数”提示。建议加入搜索（IMEI/IMEI2/单号）、状态筛选、地点筛选、清晰表头、空状态和分页或“加载更多”。
5. **查询手机没有展示轨迹。** API 已返回 `timeline`，但当前只显示状态、组织和地点。查询页面应展示“当前归属 + 托盘/箱 + 时间线”，并把动作、来源/去向、单号、操作人和时间翻译成中文，便于盘点和异常追责。
6. **重复扫码反馈不明显。** 重复编码目前被静默忽略；建议显示“已跳过重复编码”并短暂高亮已有行。无效 IMEI 应保留错误原因（校验位/长度）并自动聚焦输入框。
7. **大批量提交缺少确认和结果摘要。** 提交前应显示“本次 X 台手机 / Y 个容器、来源、目标”，对不可逆动作（发运、销售确认、退回接收、盘点调整）要求一次确认；成功后显示单号、成功/异常数量和下一步交接提示。
8. **容器操作需要更明确的物理动作提示。** 深圳装托/装箱已实现先扫目标容器，但页面仍把目标容器输入框和扫描列表放在同一层。建议按“①扫描托盘/箱 → ②连续扫描手机/托盘 → ③封存确认”分段，并在重新扫描容器时明确旧批次将被清空。
9. **目标地点回退到全部地点存在误选风险。** `destinationLocations` 在没有匹配类型时回退全部地点，可能把手机发往错误类型的地点。建议显示“没有符合业务类型的目标地点”，让管理员修正配置；不要静默放开全部地点。
10. **权限不足与空数据未区分。** 没有权限时当前工作台常显示 0，而不是“当前岗位没有查询权限”。建议区分加载中、无权限、无数据和网络错误四种状态；导航隐藏权限入口时在个人岗位信息中展示可执行范围。
11. **错误处理缺少字段定位。** 后端返回的错误被统一放在页面顶部，扫码员在长页面中不容易看到。建议同时在相关字段下显示错误，顶部保留摘要，并用 `role="alert"` 通知辅助设备。
12. **跨岗位交接缺少确认信息。** 发运/调拨/退回/维修建单成功后应显示交接对象、箱/托盘数量、手机数量、封签/物流号（若有）以及“待对方接收”状态，避免操作员误以为库存已到达。

### P2：可在后续迭代优化

- 登录增加可见/隐藏密码、`autocomplete`、账号格式示例和加载文字；
- 管理员用户角色不要依赖 Ctrl/Cmd 多选，改成带搜索的复选卡片；角色权限不要手输逗号代码，改成按权限分组的复选框；数据范围 JSON 改为组织/地点选择器，并保留高级 JSON 入口；
- 管理页组织、地点、角色和审计表增加表头、搜索、状态筛选、操作确认和最近更新时间；停用组织/地点前显示影响用户和库存的提示；
- 单据状态采用中文状态徽章并统一颜色，避免直接展示后端枚举；时间统一为本地时区格式；
- 扫码输入增加 `autocomplete="off"`、`spellcheck="false"`、IMEI `maxlength=15` 和粘贴批量导入入口；
- 价格字段改为按台明细输入（货币、金额格式、非负校验），不要仅依赖逗号分隔字符串；
- 离线草稿显示创建时间、地点、批次内容、失败原因和重试进度；重试全部提供进度与停止按钮；
- 为所有按钮补充 `aria-label`/`aria-live`，为表格补充 caption 和移动端卡片视图；
- 超宽显示器设置内容最大宽度，避免工作台控件离得过远；
- 将“扫码枪就绪”改为“在线 · 扫码输入已就绪”，避免让员工误以为系统能检测硬件连接状态；
- 对维修完成/QA、盘点差异和停用操作增加二次确认及审计说明。

### 浏览器走查补充（与源码结论交叉验证）

在登录后的 PC 页面按岗位入口检查后，确认以下问题曾在实际渲染中出现：

- 管理页曾把 `v-for` 与 `v-if` 写在同一个按钮上，Vue 运行时会提示 `tab` 未定义并可能绕过按钮权限过滤；本轮已改为 `template v-for` 包裹按钮；
- 调拨收货阶段的容器类型选择会造成“托盘/箱子”被送入只接受 IMEI 的接口，本轮已隐藏该阶段选择器并固定手机逐台扫描；
- 选择手机模式时，原 `scanMode` 仍按任务默认的 container 模式判断，任意字符串可进入待提交清单；本轮已改为按 `containerKind` 选择 IMEI/容器校验；
- 顶部在线状态原文案“扫码枪就绪”不能代表硬件已连接，本轮改为“在线 · 扫码输入已就绪 / 离线模式 · 可继续录入”；
- 切换独立任务或综合岗位时旧单号/目的地可能残留，本轮增加操作字段清理，避免误提交到上一单据。

## 验证

- `cd frontend-pc && npm run build`：通过（Vite 6.4.3，13 modules，产物约 120.54 kB JS / 8.13 kB CSS）。
- `git diff --check`：通过。
- 本轮未修改后端、数据库或共享 API 合同；仅修改 PC 前端提交提示和 PC 样式。

## 后续建议

主模板重构时优先处理 P0/P1 的字段标签、表格结构、查询轨迹和离开确认；完成后用 320px、768px、1366px 三种窗口宽度分别走一遍深圳、加纳、门店、维修和管理员账号，并验证扫码枪“无回车”和“带回车”两种配置。
